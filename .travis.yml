os: linux
language: rust
sudo: true
env:
  global:
    - LD_LIBRARY_PATH=/usr/local/lib
addons:
  apt:
    packages:
      - libgtk-3-dev
rust:
  - stable
  - beta
  - nightly
script:
  - cargo build
notifications:
  email:
    on_success: never
